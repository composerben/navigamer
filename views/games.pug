extends layout.pug

append head 
  link(rel="stylesheet", href="stylesheets/games.css")

block content
  div#games__container 
    each game in games
      div.body__gameCard 
        a(href=`../games/${game.id}`) 
          img(src=game.imgUrl)
          h2= game.gameName
